# 环境与版本

更新时间：2026-08-24

## 宿主机基线

- OS：Ubuntu 24.04.3 LTS
- Kernel：6.17.0-22-generic
- Conda：26.1.1，路径 `/home/uav/miniconda3`
- Docker 客户端：已发现；沙箱内未连接 daemon
- GPU：NVIDIA GeForce RTX 5060 Ti
- NVIDIA Driver：580.126.09
- OpenGL：4.6.0 NVIDIA 580.126.09
- 沙箱内 NVIDIA 设备未映射；沙箱外 EGL 渲染验证成功
- `/` 可用空间：约 197 GB（执行前基线）
- 目标项目空间预算：不超过 100 GB，建议控制在 60–80 GB

## 第三方版本

- FALCON：`312eb4d32c6c7af1a482f94a0a204aa2bb150cca`
- Boxer：`1f86542dc342a4b1d474c87c97c5d1d6566d9148`
- OccuSG：`2bca2fa06af87fd9dd0542957039be8f580f0dca`
- Habitat-Lab stable：`cdbb4880519505adf45fba0f0c0c3a3fd18a2a55`
- Open3D v0.18.0：`0f06a149c4fb9406fd3e432a5cb0c024f38e2f0e`
- NLopt v2.7.1：`09b3c2a6da71cabcb98d2c8facc6b83d2321ed71`

完整 URL 和复现信息见仓库根目录 `third_party.lock.yaml`。

## Habitat 主环境

- 路径：`.envs/habitat`
- Python：3.9.23
- Habitat-Sim：0.3.3，Bullet `True`
- Habitat-Lab：0.3.3 editable install
- CMake：3.27.9
- NumPy：1.26.4
- Pillow：10.4.0

## Boxer 权重

路径：`third_party/boxer/ckpts/`（主 Git 忽略）。

- BoxerNet：`e195aac4badb543e8652c11b6fa1681607544c3a20d78bd16bdbfb4164b3c437`
- DINOv3：`1edbaf93ae4798b6d64f1d860fe472be15c49f9d53a5f4406f4971208add28c0`
- OWLv2：`14aa78ffe7b13e5b3ebf55845bc9a07e339a095cfd88f4c4e8f726b38ce1ebbf`
- 三个文件均通过 `torch.load(..., map_location="cpu")` 验证。

## 当前开发场景

- 数据：HM3D-Semantics v0.2 official example
- Scene ID：`00861-GLAQ4DNUx5U`
- RGB mesh：`GLAQ4DNUx5U.basis.glb`
- Semantic mesh：`GLAQ4DNUx5U.semantic.glb`
- Semantic objects：908
- 单帧可见 semantic IDs：24（首次烟雾测试）

官方 example 包还包含 `00337-CFVBbU9Rsyb` 与 `00770-NBg5UqG3di3`，已用于第二阶段真实 navmesh 几何规划回归。

## 第二阶段服务与运行时

- DashScope model：`qwen3.7-plus`，OpenAI-compatible endpoint，非思考结构化 JSON 模式。
- Qwen 预算上限：20 元；5 次调用后的 ledger 保守估算累计 0.056318 元。
- Habitat 在线执行继续使用 `.envs/habitat` 与 RTX 5060 Ti；实测 EGL/OpenGL 4.6 正常。
- ROS bag 构建/RViz 继续复用项目专属 FALCON Noetic 容器，不安装宿主机全局 ROS。

## Boxer 环境

- 路径：`.envs/boxer`
- Python：3.12.14
- PyTorch：2.13.0+cu130
- CUDA Runtime：13.0
- NumPy：2.5.2
- OpenCV：5.0.0.93
- GPU验证：`torch.cuda.is_available() == True`，RTX 5060 Ti 张量求和测试通过

## FALCON / OccuSG 容器

- FALCON：Ubuntu 20.04 + ROS1 Noetic，镜像 `pre-map-vln/falcon-noetic:local`，约 4.94 GB。
- OccuSG：Ubuntu 22.04 + ROS2 Humble，镜像 `pre-map-vln/occusg-humble:local`，约 2.26 GB。
- OccuSG 编译：`colcon --parallel-workers 1`，C++ 上限 2 workers。

## 当前占用（2026-08-23 验收后）

- `PRE_MAP_VLN` 总占用：约 16 GB
- 本项目目录：约 16 GB。
- 本项目两个主镜像逻辑大小合计约 7.20 GB（共享层会影响物理占用统计）。
- 系统分区剩余：约 177 GB。
- 低于项目 100 GB 预算；全局 Docker 中另一个项目的旧容器/镜像不计入本项目且未清理。
