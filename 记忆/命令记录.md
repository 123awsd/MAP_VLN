# 命令记录

## 2026-08-23：初始化

- 创建 `PRE_MAP_VLN` 标准目录。
- 在 `PRE_MAP_VLN` 初始化独立 Git 仓库。
- 未修改宿主机全局 Python、CUDA、ROS 或另一个项目。

## 2026-08-23：第三方源码

- 下载 FALCON、Boxer、OccuSG（含子模块）、Habitat-Lab stable、Open3D v0.18.0 和 NLopt v2.7.1。
- 将确切 commit 写入 `third_party.lock.yaml`。
- 第三方源码位于被主仓库忽略的 `third_party/`。

## 2026-08-23：模型与 Habitat

- 下载 BoxerNet、DINOv3、OWLv2 权重；发现一次续传污染后重下 OWLv2 并完成反序列化验证。
- 在 `.envs/habitat` 安装 Python 3.9.23、Habitat-Sim 0.3.3、Habitat-Lab 0.3.3 和 Bullet。
- 修正 Pillow 到 Habitat-Sim 要求的 10.4.0。
- 下载 `hm3d_example` v0.2，包括 Habitat mesh、configs 和 semantic annotations。
- 使用 `scripts/smoke_habitat.py` 在 RTX 5060 Ti 上导出 RGB、Depth 和 Semantic。

## 2026-08-23：Boxer运行环境

- 在 `.envs/boxer` 创建 Python 3.12.14 独立环境。
- 安装 PyTorch 2.13.0+cu130、CUDA 13 runtime、NumPy、OpenCV、tqdm 和 dill。
- `pip check` 无损坏依赖，GPU张量测试成功。
- 尝试拉取 `nvidia/cuda:13.0.0-base-ubuntu22.04` 验证Docker GPU；Docker Hub速度降至约20KB/s后安全中断，未留下运行容器。

## 2026-08-23：FALCON 闭环

- 构建 `pre-map-vln/falcon-noetic:local`，在 ROS1 Noetic 中编译 FALCON 和文件桥。
- Habitat 以 5 Hz 写入 RGB、Depth、Semantic 和 optical camera pose。
- FALCON 实际产生 B-spline 轨迹，Habitat 按命令移动；最终验收 episode 录制 40 个关键帧、轨迹 19.322 m。

## 2026-08-23：Boxer、OccuSG 与融合

- `./scripts/run_boxer_habitat.sh data/episodes/hm3d_stage1 outputs/boxer`
- Boxer 处理 40 帧，163 个候选框融合为 2 个静态实例。
- `scripts/build_occusg_grid.py` 生成 256×321、0.05 m/pixel 栅格。
- `./scripts/run_occusg.sh hm3d_stage1` 输出 11 个区域、11 条邻接边，处理 30.4 ms。
- `scripts/fuse_rooms_boxes.py` 输出 `outputs/scene_graph/hm3d_stage1.json`。
- `scripts/validate_stage1.py` 返回 `status: passed`。

## 资源安全

- OccuSG 镜像最终使用 `./scripts/build_occusg.sh` 构建。
- 预检时可用内存 24.4 GiB、swap 7.7 GiB；编译中确认最多 2 个 `cc1plus`。
- 构建完成后内存与 swap 恢复正常，没有清理或删除全局 Docker 资源。

## 2026-08-23：RViz 与 rosbag

- 扩展 `file_bridge_node.py`，发布 `/habitat/rgb`、`/pre_map_vln/agent_path` 和 `/pre_map_vln/box_markers`。
- 新增项目 RViz 配置、实时录制 launch 和回放 launch。
- 首次 X11 授权失败产生 547 MB 无效包，确认目标后删除；没有删除其他项目数据。
- 使用临时、Git 忽略的 `runtime/rviz.Xauthority` 传递当前桌面 cookie。
- 正式运行 45 秒 Habitat–FALCON 闭环并正常封包。
- `rosbag info`：65.383 秒、26,222 messages、LZ4、141 MB、indexed true。
- 10 秒 RViz 回放成功，OpenGL 使用容器 llvmpipe 3.1。

## 2026-08-24：RViz 可视化修正

- 将 optical sensor pose 与 body odometry 姿态分开，Habitat 跟随时默认面向实际移动方向。
- 录制前清理项目内明确的瞬时桥文件，避免旧帧导致 FALCON 地图状态跳变。
- Boxer 调试融合使用 `iou=0.2, min_detections=2, conf_threshold=0.3`，得到 26 个去重 box。
- 最终回放包 35.176 秒、15,103 messages、LZ4、93 MB，首帧 RGB 立即可见。
- RViz 截图验收：彩色线框/标签可见，occupied cloud 为 1 px/alpha 0.22，depth cloud 和 candidate viewpoints 默认关闭。

## 2026-08-24：同回合渐进 Box 回放

- `./scripts/record_progressive_stage1.sh hm3d_stage1_progressive 40 10`：同时录制 10 Hz bag 与 2 Hz Boxer 关键帧。
- Habitat 生成 400 帧，bag 收到 399 帧 RGB-D；实测 9.95 Hz。
- Boxer 处理 80 个同回合关键帧，生成 294 个原始 3D 框、6 个正式高置信实例和 31 个调试实例。
- `inject_progressive_boxes.py` 自动裁到首末 RGB，按 18 个 `first_seen` 时刻注入累计 MarkerArray。
- 最终 bag：40.246 秒、5,326 messages、LZ4、161 MB。
- 回放验证：Box 计数 `1→5→6→7→…→31`；10 倍速播完后 RViz 仍保持运行。

## 2026-08-24：连续 RGB 与双视图平衡版

- 逐帧诊断旧包：399 帧持续约 10 Hz 发布，但有 159 对 byte-identical RGB；9 个静止区间累计约 15.8 秒，最长 4.3 秒，期间 odometry 位移为 0。
- 原因是 FALCON 等待/重规划时 Habitat 位姿不变，不是 ROS 丢图；周期发布的 `/planning/travel_traj` 造成“轨迹在走而相机卡住”的错觉。
- `run_habitat_falcon.py` 新增静止期主动扫描；移动时仍按真实运动方向调整镜头。
- 首次主动扫描回合触发 FALCON 的近零网格代价 fatal check；新增 `zero_cost_guard.patch` 将数值重合边夹为极小正代价，并重建隔离镜像。
- `./scripts/record_progressive_stage1.sh hm3d_stage1_scan_complete 40 10 25` 完成修补后的完整回合录制、Boxer 与渐进 Marker 注入。
- 最终 bag：39.998 秒、5,560 messages、约 182 MB；400 帧 RGB、10.0 Hz，最长重复画面降到 0.2 秒，occupied map 更新持续到末帧前 0.007 秒。
- Boxer 从 326 个原始框得到 27 个调试实例；bag 注入按地图距离门控排除 chair/shelf 两个离群框，25 个可视框按 19 次首次观测渐进出现，原始 CSV 保留不变。
- 新增 `stage1_topdown.rviz`；3D/RGB 与 2D 边界/frontier 同时显示，播放前等待订阅者 2 秒，播完两个窗口均保持。

## 2026-08-24：掀顶式实时探索视图

- 新增 `roofless_visualization_node.py`，实时过滤 `/voxel_mapping/occupancy_grid_occupied` 并发布 `/pre_map_vln/roofless_map`。
- 默认只保留世界 Z=0.50–2.25 m：默认包末帧 46,696 个 occupied 点中剔除 13,180 个地板/低层点和 11,328 个顶棚点，保留 22,188 个墙体/主要障碍点。
- 节点订阅 `/planning/bspline`，用 de Boor 算法采样 64 点并发布黄色 `/pre_map_vln/current_plan`；每次新规划覆盖旧 Marker。
- 实机回放确认派生节点、RGB、主/俯视 RViz 同时运行；抽检帧的 roofless cloud 为 18,388 点，当前规划 Marker 类型为 `LINE_STRIP`。
- 主 3D 关闭地面网格和红色命令历史；绿色实际轨迹、黄色当前规划、低透明橙色 Frontier、细 Box 与 RGB 分层显示。
- 实时录制 launch 也默认支持双 RViz；原固定高度参数已在下一次修改中替换为斜顶自适应参数。

## 2026-08-24：斜顶自适应过滤与 FALCON 风格

- 将固定世界 Z 截断改为逐 XY voxel 柱的局部上包络过滤；地板高度由 `/uav_simulator/odometry` 与传感器高度推断。
- 默认参数为离地净空 0.25 m、顶棚最小高度 1.65 m、顶层剥离厚度 0.60 m；无需 SciPy。
- 默认 bag 末帧由 46,696 点过滤为 20,168 点；局部顶面 Z 跨 1.55–3.75 m，可覆盖斜顶而不把它当成水平面。
- 首版白底深灰 Boxes + 大尺寸 RGB8 Frontier 仍显得厚重且杂乱；按用户指定的 FUEL `office3.gif` 再改为派生点云内受控的青—蓝—洋红高度配色 Points、深蓝细轨迹和 1 px 半透明分类色 Frontier，避免 RViz 默认完整彩虹，俯视窗口也使用已过滤地图。
- 回放脚本第 5/6/7 参数分别覆盖离地净空、顶棚最小高度和局部剥离厚度。
- 增加实际相机视锥：订阅 bag/实时共有的 `/uav_simulator/sensor_pose`，按 ROS optical frame 的 +Z 前向生成 90°×4:3 红色 `LINE_LIST`，发布到 `/pre_map_vln/camera_frustum`。

## 2026-08-24：FSM 驱动的完整探索重录

- 证实旧 `scan_complete` 是固定 40 秒截断：末帧仍有 8,056 个 active Frontier 点。
- 首次自动回合因地面 navmesh 与 UAV 规划冲突而在两个视点振荡，约 87 秒时主动停止，保留 `hm3d_stage1_complete_v2_raw.bag` 作为失败证据。
- UAV 执行器改为精确跟踪 FALCON PositionCommand 后，以 `hm3d_stage1_complete_v3` 重跑；约 242 秒 FALCON 报告 `No frontier detected` 并进入 `FINISH`，3 秒收尾后结束。
- 最终 bag：248.303 秒、约 1.7 GB、43,379 条消息、2,479 帧 RGB、420 次地图更新；末帧 active Frontier=0、occupied map=234,056 点、实际轨迹 2,479 poses。
- Boxer 处理 497 个关键帧，从 2,177 个原始 OBB 得到 179 个渐进实例，按 118 次事件注入最终包。
- 8 倍速双 RViz 回放检查通过：墙体覆盖明显完整，RGB、轨迹、Frontier、视锥和渐进 Box 正常，播放完成后窗口保持。

## 2026-08-24：主视图样式与入口精简

- Box 不重写完整 bag，而由派生节点订阅原始 `/pre_map_vln/box_markers` 并发布 `/pre_map_vln/styled_box_markers`；初验 0.017 m 后按反馈微调为 0.020 m，颜色通道乘 0.72、alpha 至少 0.62。
- `replay_bag_rviz.sh`、录制入口和两个 launch 均把 2D 俯视默认值改为关闭；第三参数继续统一控制 rosbag 回放倍速。
- 为唯一 Git 根目录添加 `origin=https://github.com/123awsd/MAP_VLN.git`，只配置远端，未推送。
- 完整包 20 倍速回放抽检：launch 只启动主 RViz；派生消息为 `width=0.020`、`rgba=0.086,0.518,0.720,0.620`，渐进 MarkerArray 正常发布。

## 2026-08-24：第二阶段核心闭环

- DashScope/Matterport 凭据写入 `.secrets/`，未进入 Git；`qwen3.7-plus` 真实解析得到 5 tasks、1 precedence、1 condition，费用估算 0.008608 元。
- 将完整回合 179 个 Boxer box 与 11 个 OccuSG 区域融合为开发 scene graph。
- 现有地图实测联合规划 4.74 m，固定顺序基线 7.05 m，缩短 32.8%。
- `observe_living_room_tv=not_found` 时正确激活 cabinet 并重规划：5 visits、6.91 m；found 时正确跳过：4 visits、4.74 m。
- `python3 -m unittest discover -s tests -v`：初版 10 tests passed。

## 2026-08-24：第二阶段完整在线闭环与可视化

- `./scripts/run_stage2_demo.sh`：依次融合 scene graph、调用 Qwen、生成候选、联合规划、Habitat GPU 在线执行、构建 bag 和自动验收。
- Qwen 最终 schema 把导航锚点与 `verification_label` 分离；默认指令得到 5 tasks、1 precedence、1 condition；课题定义正式示例另得到 5 tasks、1 condition，ledger 最终累计保守估算 0.056318 元。
- 初始 4 个活动任务：联合路径 4.466 m，固定顺序 6.576 m，缩短 32.09%。
- Habitat 执行 5 tasks/5 replans/112 RGB frames/7.569 m；电视受控阈值触发 cabinet 条件任务，chair/door/cabinet 验证为 found。
- Docker 构建 `hm3d_stage2_complete.bag`：22.4 秒、599 messages、70,664,990 bytes。
- `./scripts/run_stage2_multiscene.sh`：三个官方 example 的真实 navmesh 几何规划和动态任务状态机全部完成。
- `./scripts/validate_stage2.py`：`status=passed`；最终 `python3 -m unittest discover -s tests -v` 为 12 tests passed。
- RViz 运行检查确认 RGB、掀顶地图、执行/全局/局部轨迹、候选/选中位姿、语义 box、UAV 与实际相机视锥同时显示；bag 完成后窗口保持。

## 2026-08-24：第三阶段论文评测

- `./scripts/run_stage2_experiments.sh`：运行 3 scenes × 3 seeds × 5 cases × 6 methods。
- 首轮发现 `00337` 随机高度可能落到 4.13 m 小型楼层，造成无解 anchors；失败报告被保留用于定位，但未作为结果。
- 修复为扫描 navmesh 高度并选择最大连通楼层，同时按最小观察环 free/LOS 可行性筛选 anchors。
- 修复后连续完整运行达到 270 records、0 failures。
- 公平计时版为每种方法清空 A* cache；目标函数扩展为 `L + 0.25T + 2.0C_view`。
- `python3 -m unittest discover -s tests -v`：15 tests passed。

## 2026-08-24：本地开放词表感知闭环

- 新增 `.envs/boxer` 中常驻的 OWLv2 worker；Habitat 进程以 JSON-lines IPC 调用，避免每帧重复加载 881 MB 权重，也避免两个虚拟环境混装。
- 五个终点帧离线烟雾测试：lamp/chair/door/television/cabinet 全部命中；启动 4.13 s，首帧 86 ms，随后约 35–39 ms。
- Habitat 全闭环命令使用 `--verification-mode owlv2 --owlv2-every 10 --owlv2-threshold 0.20`；结果为 completed、4 tasks、4 replans、74 frames、5.383 m、6.54 s。
- RGB 检测框通过对齐 depth 中值与相机位姿投影为世界坐标 3D boxes；飞行中 8 个检测关键帧产生 16 个同类 scene-graph 关联确认框，4 个远距离投影作为未关联证据保留但不进 RViz 在线框层。
- Docker 生成 `hm3d_stage2_open_vocab_complete.bag`：14.8 秒、411 messages、45,246,851 bytes，`/stage2/open_vocab_boxes` 有 8 次渐进更新。
- `validate_stage2.py` 对 found→skip 和 not_found→activate 两种条件分支均可验收；本地开放词表运行 `status=passed`；`python3 -m unittest discover -s tests -v` 为 22 tests passed。
- Qwen-VL 的 5 帧与在线终点实验保留为可选感知对照，总审计费用 0.068445 元；不再作为默认或连续检测器。

## 2026-08-24：异步融合、恢复和三地图感知 pilot

- `run_stage2_habitat.py --owlv2-every 5`：异步闭环 4 tasks/74 frames/5.383 m/6.3 s；提交 9 次、回收 9 次、忙时跳过 6 次，检测均值 42.30 ms。
- 多帧 fusion：13 tracks、10 confirmed pre-map associations、3 unconfirmed single-view novel candidates、0 false novel insertions。
- `--owlv2-threshold 0.90 --max-viewpoint-attempts 2` 受控恢复：10 visits、5 retries、251 frames、14.94 m；TV not-found 后 cabinet 激活，全部任务有界结束。
- `evaluate_open_vocab_multiscene.py --views-per-scene 48`：三场景 144 帧；平均/p95 35.78/36.89 ms；`00861` Semantic-GT micro precision/recall/F1 为 0.658/0.481/0.556。
- 阈值扫描 0.20–0.60；全局 F1 最佳 0.20，pilot 类别阈值为 bed 0.325、lamp 0.25、television 0.45、shelf 0.425。
- 融合版 bag：`hm3d_stage2_perception_v2_complete.bag`，14.8 秒、412 messages、在线框累计 `1→3→4→4→5→7→7→10→10`。
- `scripts/validate_stage2_perception.py`：`status=passed views=144 f1=0.556 p95=36.9ms retries=5`；32 tests passed。

## 2026-08-24：最终条件任务与语义恢复搜索

- `.envs/habitat/bin/python scripts/prepare_final_demos.py ... --output-root outputs/final_demos`：生成 3+3 固定任务图与候选。
- 6 次 `scripts/run_stage2_habitat.py`：均 completed；恢复任务启用 `--semantic-recovery --max-recovery-hypotheses 3 --max-recovery-visits 8 --max-viewpoints-per-location 2`。
- 6 次 Docker `build_stage2_bag.py`：输出 `outputs/bags/final_demos/*.bag`，每帧 RGB 完整。
- `.envs/habitat/bin/python -m unittest discover -s tests -p 'test_stage2_*.py' -v`：37 tests passed。
- `.envs/habitat/bin/python scripts/validate_final_demos.py`：passed，6 demos，bag 合计 1,719,882,128 bytes，Qwen 台账 0.083801 元。
- `./scripts/replay_stage2_rviz.sh final_demos/recovery_01_tv true 2.0`：实际循环回放通过，RViz 保持打开；截图位于 `outputs/final_demos/recovery_01_tv/rviz_window.png`。

## 2026-08-25：房间规范化与官方几何对照

- `.envs/habitat/bin/python scripts/validate_stage1.py --episode hm3d_stage1_complete_v3 --output outputs/room_comparison/stage1_validation.json`：验收单层规范场景图，6 rooms + 4 corridors + 0 canonical fragments + 28 unique objects。
- `.envs/habitat/bin/python scripts/compare_room_segmentation.py`：生成 `outputs/room_comparison/hm3d_stage1_complete_v3.png` 及 JSON 对照报告。
- `./scripts/run_final_demos.sh`：6 个 Habitat demo 和 bag 按单层场景图重建；加入 28 个常驻语义 Box 标签后六包合计 1,865,370,372 bytes，Qwen 台账 0.104527 元。
- `.envs/habitat/bin/python -m unittest discover -s tests -v`：50 tests passed。
- `.envs/habitat/bin/python scripts/validate_stage1.py` 与 `scripts/validate_final_demos.py`：均 passed。
- `./scripts/replay_stage2_rviz.sh final_demos/recovery_01_tv true 1.0`：循环打开单层地图，验证 `/stage2/semantic_boxes` 含 28 个常驻 `stage2_context_labels`。
