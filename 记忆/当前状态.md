# 当前状态

更新时间：2026-08-25

## 当前结论

第一阶段预探索、第二阶段条件长指令与目标缺失后的 VLM 语义恢复搜索均已形成可运行闭环。跨多次家庭任务学习物品位置规律、三档持久性升级和家庭习惯建模已冻结，不是当前待办。

唯一 Git 根目录是 `/home/uav/map_VLN/PRE_MAP_VLN`；未修改另一个项目或宿主机全局 Python/ROS/CUDA。远端为 `https://github.com/123awsd/MAP_VLN.git`。

## 第一阶段

- Habitat + FALCON 自主探索完成，结束依据为 FSM `FINISH` 与 active Frontier=0。
- 完整包：`outputs/bags/hm3d_stage1_complete_v3_final.bag`，248.303 秒、2,479 帧 RGB、约 1.7GB。
- Boxer/OccuSG 融合开发场景图：单层规划层为 6 个规范房间、4 个 corridor、0 个 fragment、28 个唯一物体；2 个源碎片仅保留在 `geometric_regions` 追溯字段。
- 算法不支持楼梯/跨层；建图使用 0.15–1.8m 单层高度带，HM3D `stairs/stairs railing` 组件以 0.20m 净距屏蔽 3,296 个栅格。
- RViz：白底掀顶点云、无地板/顶棚、RGB、实际轨迹、Frontier、FoV、渐进 3D Box。

## 第二阶段既有基础

- Qwen3.7-Plus task graph 解析、缓存、schema/DAG 校验、密钥隔离与费用审计。
- prerequisite、conditional 和无序活动任务；A* + Subset-DP 联合任务顺序和多终止位姿。
- 本地 GPU OWLv2 异步开放词表检测、RGB-D 投影、三维融合和同位置多视角恢复。
- 3 scenes × 3 seeds × 5 cases × 6 methods = 270 条规划评测，0 failures；结论边界见 `docs/第三阶段实验评测.md`。

## 本次完成：VLM 语义恢复搜索

- `task_graph` 新增 `search_policy=fixed|semantic_recovery`。
- 预期位置最多观察若干视角，位置耗尽后过滤其余同位置候选。
- Qwen 根据缺失目标、失败位置和全局房间—物体清单生成最多 3 个候选。
- 只接受真实 `room_id`/`anchor_object_id`；拒绝虚构、错误房间、重复和已失败位置。
- Qwen 不生成坐标；观察位姿由几何模块生成并通过 free-space、LOS 与 A* 可达性检查。
- 同一目标一次恢复最多调用一次，结果缓存并写入 `outputs/stage2/api_usage.json`。
- 恢复候选进入原联合 planner；每个位置结果后滚动重规划，first-success 或预算耗尽 not_found。

## 最终 6 个 demo

自动报告：`outputs/final_demos/report.json`，状态 passed。

- `conditional_01_branch`：分支触发，32.682m，578 帧。
- `conditional_02_skip`：分支跳过，33.132m，602 帧。
- `conditional_03_parallel`：无序任务 + prerequisite + fallback，51.679m，936 帧。
- `recovery_01_tv`：Qwen 3 个假设，受控 found，21.057m，367 帧。
- `recovery_02_lamp`：Qwen 3 个假设，受控 found，18.445m，340 帧。
- `recovery_03_exhausted`：Qwen 3 个假设多位置搜索后 not_found，27.974m，477 帧。

所有受控结果都写入 execution trace，不冒充真实检测精度。六个独立包位于 `outputs/bags/final_demos/*.bag`，合计 1,865,302,309 bytes（约 1.87GB）。每包包含连续 RGB、掀顶地图、橙色实际轨迹、UAV/FoV、动态任务框、状态五角星及 `/stage2/recovery_search`。恢复激活后才显示红色旧位置 X 与排序候选，不提前泄露。

Qwen 共用台账当前估算约 0.104527 元，低于 20 元授权上限。全仓库 50 项单元测试全部通过。

## 2026-08-25 房间规范化修复

- 修复同一房间的 OccuSG 小碎片仍被 RViz/Qwen 当成独立房间的问题。
- 最终家具碎片合并为 R1→R2 bedroom、R5→R14 kitchen。
- 规范场景图现为 10 个规划区域：6 rooms + 4 corridors；2 个源 fragment 不再进入 `rooms`。
- 原先被当成第 7 个房间的斜形区域实为楼梯/楼梯井投影，已从单层占据图和任务图排除。
- 碎片物体、邻接关系和多边形组件均归入父房间，保留 `source_region_id`、`merged_from_region_ids` 和 `geometric_regions` 供审计。
- 新增 `scripts/compare_room_segmentation.py`，对齐显示 Habitat 官方 navmesh、探索占据图和规范房间分割。
- 对照图：`outputs/room_comparison/hm3d_stage1_complete_v3.png`。
- 六个 Habitat demo 与 bag 已按新场景图重建；报告 passed。Qwen 共用台账更新为约 0.104527 元。
- 全仓库 50 项测试通过。

## 最常用命令

```bash
cd /home/uav/map_VLN/PRE_MAP_VLN

# 全量重建六 demo（需要 GPU、网络、Docker/ROS）
./scripts/run_final_demos.sh

# 只验收现有输出
.envs/habitat/bin/python scripts/validate_final_demos.py

# loop=true，播放完不会自动退出
./scripts/replay_stage2_rviz.sh final_demos/conditional_01_branch true 1.0
./scripts/replay_stage2_rviz.sh final_demos/recovery_01_tv true 1.0
```

## 当前 RViz 验收

恢复电视包已实际循环回放。干净窗口截图：`outputs/final_demos/recovery_01_tv/rviz_window.png`。可见 RGB、掀顶点云、动态任务框、实际橙色轨迹、红色旧位置失败标记、恢复候选和绿色到达星。

## 后续论文工作（尚未完成）

1. 显式 goal-level AND–OR schema；
2. near/on/between/above/below 的完整几何硬约束；
3. 房间不可达的显式错误，禁止静默错误房间 fallback；
4. 多真实语义场景的位置扰动评测；
5. Expected-only、Random-room、Category-nearest 与 Full recovery 对比；
6. 恢复候选数、位置视角数、预算和 VLM 等级消融；
7. 感知误差、恢复延迟/费用、置信区间和失败案例分析。

完整课题边界以 `/home/uav/map_VLN/课题文档/课题定义.md` 为准；实现说明见 `docs/第六阶段语义恢复搜索.md`。

## 阻塞项

- 正式 HM3D-Semantics minival 下载仍需要完整 Matterport token ID + token secret；现有单个值不满足官方下载认证。
- 两个密钥只位于 `.secrets/`，未进入 Git、文档或记忆。
