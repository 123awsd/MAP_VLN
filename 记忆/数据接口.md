# 数据接口

统一地图坐标系计划采用右手系、Z-up、单位米。

每个关键帧至少包含：

- `timestamp`
- `frame_id`
- RGB 图像
- 米制 Depth
- semantic instance/category
- 相机内参
- `T_world_camera`
- `T_world_agent`

坐标变换和最终 JSON schema 在 Habitat 烟雾测试后锁定。

