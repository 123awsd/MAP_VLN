# 文件变更

## 2026-08-25：离线宽词表重建

- 新增 54 类稳定室内离线词表与统一语义别名配置；第一阶段 Boxer 默认使用 OWLv2 0.20、融合 0.45、至少 4 帧支持。
- Qwen 结构策略改为最多 40 框/批，避免宽词表输出 JSON 被截断。
- 房间物体关联由无限距离最近质心改为多边形包含/最近边界/0.75 m 上限，超限物体显式进入 `unassigned_objects`。
- 房间语义增加边界距离衰减和体积类几何合理性检查；最终仍为 5 rooms + 2 corridors。
- 新 bag `hm3d_stage1_complete_v3_indoor_v1_final.bag`：89 个 Box、71 个渐进事件、2,479 RGB；默认回放切换到该包。
- 第二阶段继续只加载任务相关标签，并统一从 `config/semantic_aliases.json` 读取同义词。
- 阶段一验收通过，全仓库 59 项单元测试通过；既有第二阶段规划在新图上 smoke test 通过。

## 2026-08-23

- 建立项目骨架、Git 忽略规则和记忆系统。
- 新增第三方锁文件和依赖获取脚本。
- 新增 `scripts/smoke_habitat.py`。
- 下载的环境、数据、权重和第三方源码均被 `.gitignore` 排除。
- 新增 FALCON ROS1 Docker、Habitat 文件桥、坐标变换和动态探索 runner。
- 新增 Boxer HabitatLoader、上游 replay patch 和项目级运行包装脚本。
- 新增 OccuSG ROS2 Humble 最小镜像、栅格发布/区域捕获桥和安全构建脚本。
- 新增 RGB-D 栅格生成、房间/物体融合、阶段一自动验收脚本。
- 新增 `docs/第一阶段设计.md`，更新 README 与全部对话迁移记录。
- 扩展 ROS1 文件桥以发布 RGB、累计轨迹和 Boxer MarkerArray。
- 新增 `stage1.rviz`、可视化录包/回放 launch，以及对应的一键脚本。
- 新增 `scripts/prepare_rviz_xauth.sh`，每次启动时刷新当前桌面的 X11 授权，不修改系统级 X11 配置。

## 2026-08-24

- 新增 `stage2/semantic_recovery.py`：Qwen 全局语义候选、真实 ID 校验、缓存/费用审计与几何候选落地。
- `run_stage2_habitat.py` 接入位置级失败→VLM→动态候选→全局重规划闭环，并记录受控 stale/found 与完整恢复事件。
- `viewpoint_recovery.py` 增加单位置预算和已耗尽位置过滤；`task_graph.py` 增加 `search_policy`。
- 新增 `prepare_final_demos.py`、`run_final_demos.sh`、`validate_final_demos.py`，固化 3 个条件任务和 3 个恢复任务。
- 生成 6 个独立 ROS bag，合计约 1.72GB；`outputs/final_demos/report.json` passed。
- Stage 2 bag/RViz 新增旧位置失败 X 和排序恢复位置的 `/stage2/recovery_search`，任务框按重规划动态切换。
- 新增 5 项恢复/schema 测试，第二阶段测试总数为 37，全部通过。
- 新增 `docs/第六阶段语义恢复搜索.md`；重写当前状态记忆并更新 README。
- 重写 `/home/uav/map_VLN/课题文档/课题定义.md`，冻结跨任务位置规律学习，主线改为目标缺失后的 VLM 语义恢复。

## 2026-08-25

- `build_occusg_grid.py` 增加单层高度带和 HM3D 楼梯/栏杆稠密语义投影，同一楼梯组件屏蔽 3,296 个栅格。
- `fuse_rooms_boxes.py` 增加房间规范化和楼梯旁孤岛排除，最终规划层为 6 rooms + 4 corridors + 0 fragments。
- 新增 `geometric_regions`、`polygon_components_xy_m`、`merged_from_region_ids` 及物体来源字段，保留 2 个源碎片证据而不泄漏到规划层。
- `semantic_recovery.py` 与最终验收加入碎片/非规范房间防护。
- 新增 `compare_room_segmentation.py` 及 `docs/房间分割与真值对比.md`，对齐 Habitat 官方 navmesh、探索占据图和规范房间图。
- 重建 6 个最终 Habitat demo 及 ROS bag；全仓库 50 项测试和阶段验收通过。
- Stage 2 背景语义 Box 新增常驻 `类别 [boxer_id]` 文字；任务 Box 的状态文字与粗洋红框保持独立。

- 整体重写 `/home/uav/map_VLN/课题文档/课题定义.md`：补全课题名称、背景、问题形式化、自然任务示例、五阶段架构、AND–OR schema、滚动全局规划、跨任务三档位置记忆、创新点、研究问题、实验/消融、当前基础、待完成项、风险和工程边界；严格区分已实现与拟扩展内容，未写入任何真实凭据。

- 新增 `stage2/` 核心包：task graph 校验、Qwen VLM 解析、占据栅格 A*、候选终止位姿、Subset-DP 联合规划和动态任务状态机。
- 新增 `configure_secrets.py`，密钥仅保存到被忽略的 `.secrets/`，目录/文件权限为 700/600。
- 新增 `parse_stage2_instruction.py`、`plan_stage2_mission.py`、`simulate_stage2_execution.py` 入口和对应单元测试。
- 新增 `run_stage2_habitat.py`：真实 Habitat RGB/Depth/Semantic 执行、终止验证、scene graph 更新和逐任务重规划。
- 新增 `build_stage2_bag.py`、`stage2.rviz`、`stage2_replay.launch` 与 `replay_stage2_rviz.sh`，形成第二阶段完整 RViz/bag 回放。
- 新增 `benchmark_stage2_maps.py`、`run_stage2_multiscene.sh` 和 `validate_stage2.py`，覆盖三地图几何回归与统一自动验收。
- 新增 `run_stage2_demo.sh` 一键全链路入口；新增 `docs/第二阶段设计.md`，同步更新 README 和全部迁移记录。
- task graph 新增 `verification_label`、`on` 关系、缓存读取规范化和失败调用费用入账；候选与规划代码兼容当前 Python 3.9 Habitat 环境。
- 新增 `stage2/benchmark_suite.py`：四类 task suite、六种统一规划方法、动态执行与论文指标。
- 新增 `run_stage2_experiments.py/.sh`：真实 navmesh 多场景多种子 270-trial 实验、JSON/CSV/Markdown 自动汇总。
- `joint_planner.py` 的目标函数增加飞行时间、升降/yaw 约束和尺度更明确的观察质量权重。
- `validate_stage2.py` 增加论文实验的 trial、方法、成功率、A*/Euclidean 和终止质量消融门槛。
- 新增 `tests/test_stage2_benchmark.py` 与 yaw 时间测试；新增 `docs/第三阶段实验评测.md`。
- 新增 `boxer_ext/open_vocab_worker.py` 与 `stage2/open_vocab_detector.py`，形成隔离 Boxer 环境的常驻本地 OWLv2 服务、目标匹配及 RGB-D 世界坐标投影。
- `run_stage2_habitat.py` 默认切换到 OWLv2，支持在线关键帧检测、终点验证、Semantic GT 对照及可选 Qwen fallback。
- 新增开放词表评测/一键运行脚本与 3 个几何/匹配测试；保留 Qwen-VL verifier 作为显式对照实验。
- Stage 2 bag/RViz 新增 `/stage2/open_vocab_boxes`，随在线观测渐进显示本地检测产生的 3D box。
- 新增 `AsyncOpenVocabularyDetector`，以单线程有界队列把关键帧检测与 Habitat 飞行解耦。
- 新增 `stage2/semantic_fusion.py`：已知实例多帧融合、未知实例空间聚类、两视角确认和 scene graph 写入。
- 新增 `stage2/viewpoint_recovery.py`：记录/排除失败候选并约束最大观察次数。
- 新增 `evaluate_open_vocab_multiscene.py` 与 `validate_stage2_perception.py`，覆盖 144 帧三地图 pilot、阈值扫描、异步闭环和受控恢复。
- `build_stage2_bag.py` 改为发布累计 fused tracks；新增 `docs/第四阶段开放词表感知.md` 和 8 项融合/恢复/阈值测试。
- Stage 2 RViz 启用房间与任务目标图层；`build_stage2_bag.py` 增加房间类型文字、任务框状态颜色、持续任务状态表和实际到达视点五角星。重新生成 `hm3d_stage2_perception_v2_complete.bag`（14.8 秒、417 messages），自动验收通过。
- 修正 Stage 2 房间链路误用 40 帧旧格栅的问题，统一切换到 `hm3d_stage1_complete_v3` 的 497 帧格栅；`fuse_rooms_boxes.py` 增加 corridor/目标房间分类，默认观察距离调为 1.0–2.2 m（候选中位 1.6 m）。新 bag 为 39.6 秒、1,062 messages、199 帧 RGB，自动验收通过。
- `fuse_rooms_boxes.py` 增加床遮挡定向合并、主要房间/通道/碎片三层表示和强语义线索加权；当时未排除楼梯的图输出 7 个表面主要房间，后续单层修正为 6 个。
- `candidate_poses.py` 与 `plan_stage2_mission.py` 增加跨房间远距离实例分配、稀缺目标优先和不可达实例回退，记录实际采用的 object ID。
- `build_stage2_bag.py` 改为 28 个融合静态 Box 细蓝常驻、任务 Box 统一粗洋红叠加；碎片边界弱化且不显示房间编号。
- 重录 `hm3d_stage2_complete.bag`：91.4 秒、457 帧 RGB，Habitat 实际 25.20 m，4 个初始任务全部完成。

- 修正 Habitat 跟随模式的镜头朝向，新增 body/optical 双姿态语义。
- Boxer MarkerArray 改为持续发布的高亮线框，可视化融合从 2 个扩展到 26 个去重候选。
- RViz 默认降低 occupied cloud 密度/透明度，关闭 depth cloud 和 candidate viewpoints。
- 录制脚本启动前清理明确的瞬时桥文件；回放默认指向最终裁剪 bag。
- 新增 `scripts/record_progressive_stage1.sh` 一键同回合录制/Boxer/注入流程。
- 新增 `scripts/build_progressive_boxes.py`，为融合实例保留首次/末次观测时间和支持数。
- 新增 `inject_progressive_boxes.py`，裁剪 RGB 有效时段并向 bag 注入渐进 MarkerArray。
- 回放 launch 不再将 rosbag player 设为 required，播放完成后 RViz 继续运行。
- 新增静止期主动相机扫描参数，重录 `hm3d_stage1_scan_complete` 完整回合。
- 新增 `stage1_topdown.rviz`，回放默认同时打开 3D/RGB 与 2D 边界/frontier 窗口，并在播放前等待订阅者 2 秒。
- 3D Box 默认只显示更细、更暗的 outline；实体与文字 namespace 默认关闭，agent path 与 FALCON 点状轨迹同步缩细。
- FALCON 镜像增加 `zero_cost_guard.patch`，对当前位置与候选中心数值重合时的 ATSP 边赋极小正值，避免探索进程因 `CHECK_GT` 直接退出。
- 新增 `roofless_visualization_node.py`：二进制过滤 occupied cloud 的地板/天花板，并把当前 B-spline 转成 RViz Marker。
- 主 `stage1.rviz` 改为无地板掀顶视角，加入绿色实际轨迹、黄色当前规划和低透明 Frontier；红色命令历史默认关闭。
- 实时录制与回放 launch 均启动派生可视化节点，且实时入口也可同步打开 2D 俯视 RViz。
- `roofless_visualization_node.py` 改为 odometry 地面估计 + XY 柱局部上包络，支持斜顶和厚顶棚；无新增 Python 依赖。
- `stage1.rviz` 与 `stage1_topdown.rviz` 最终参考 FUEL `office3.gif`：白底、青—蓝—洋红高度细点、深蓝细轨迹、1 px 半透明分类色 Frontier 点簇；移除大块 RGB8 Boxes 的视觉干扰。
- 录制/回放 launch 与 `replay_bag_rviz.sh` 新增 floor-clearance、ceiling-min-height、ceiling-thickness 三个可调参数。
- `roofless_visualization_node.py` 新增 `/pre_map_vln/camera_frustum`，由实际 sensor pose 生成 90°、4:3 红色线框视锥；主/俯视 RViz 默认启用。
- `file_bridge_node.py` 新增 FALCON `FINISH` 状态落盘和 `/pre_map_vln/exploration_status`；录包同时记录 `/planning/replan`。
- `record_progressive_stage1.sh` 从固定时长截断改为 FSM 完成后自动停止，600 秒仅作安全上限；超时不生成 complete 包。
- `run_habitat_falcon.py` 默认精确执行 UAV PositionCommand，新增 `--navmesh-constrained` 保留地面 VLN 模式。
- 默认回放切换到真正完成的 `hm3d_stage1_complete_v3_final.bag`。
- 新增 `/pre_map_vln/styled_box_markers` 显示层：保持渐进 Box 时序，将 outline 调为 0.020 m、RGB 乘 0.72、alpha 至少 0.62；主 RViz 切换到该话题。
- 回放和实时录制默认不再启动 2D 俯视 RViz；回放脚本第四参数仍可显式启用，第三参数用于统一调整 bag 倍速。

## 2026-08-28

- 调整 `hm3d_example.yaml` 的 HM3D 室内探索参数：候选半径 0.5–2.0 m、occupied 净空 0.3 m、B-spline 安全距离 0.3 m，并加入碰撞、无路和重复到访的连续失败阈值。
- 更新 `collision_stall_finish.patch`，新增 `frontier_collision_recovery.patch`：对失败端点做局部剔除和 frontier 重选，区分正常完成与 `collision_stall`，并防止已完成端点振荡。Dockerfile 已纳入新补丁。
- `file_bridge_node.py`、`run_habitat_falcon.py` 和 `record_progressive_stage1.sh` 传递并验证终止原因；碰撞停滞或超时不再生成伪 complete 结果。
- 重建 FALCON 镜像并完整重跑 00150：`tuned_v7` 用时 213.77 s、2,135 帧，以 `replan=2` 正常完成；单元测试 66/66 通过。
- 新增 BAGS2 产物 `hm3d_stage1_00150-LcAd9dhvVwh_tuned_v7_20260828_raw.bag`、`run_result.json`、`navmesh_vs_map.json/png`。自由空间覆盖率 66.34%，已知区域覆盖率 67.40%，自由空间一致性 84.37%。
- 重生格式有误的 `collision_stall_finish.patch`，确保碰撞恢复、规划失败剔除、重复端点剔除和 traj-server 重规划全部实际应用。
- 新增 `alternative_viewpoint_resampling.patch` 和 `frontier_average_fallback_recovery.patch`：对失败视点做 0.25 m 排除后重采样，并将层级网格的 frontier-average fallback 目标反查到真实 frontier 候选。
- 新增 BAGS2 `exploratory_v11` raw bag、run result 和 navmesh 对比 JSON/PNG；289.52 s 后正常完成，左上区域轨迹 206 个采样，free/known coverage 为 71.06%/72.88%。
- 使用当前探索逻辑完整重跑 00166，新增 BAGS2 `hm3d_stage1_00166-RaYrxWt5pR1_exploratory_v11_20260828` raw bag、run result 和 navmesh JSON/PNG；free/known coverage 为 99.18%/99.98%，130.25 s 正常完成。
- 从旧 00166 raw bag 重生缺失的 recovered 对比 JSON 及 PNG；旧回合 free/known coverage 为 96.93%/100.00%，bag 时长 191.36 s。
- `run_habitat_falcon.py` 新增 planner Z 执行模式，并将 planner yaw/Z、navmesh 模式写入 run result；`record_progressive_stage1.sh` 默认开启 planner Z 和 yaw，均可通过环境变量回退。
- `hm3d_example.yaml` 将固定 frontier 高度改为动态平均 Z，同时保留二维层级连通图；三维层级图试跑因节点数和规划耗时显著上升而未采用。
- 实时录包新增 `/planning/pos_cmd`，可审计 FALCON 三维位置/yaw 与 Habitat 实际姿态的一致性；已重建 `pre-map-vln/falcon-noetic:local` 镜像。
- 新增 BAGS2 `hm3d_stage1_00166-RaYrxWt5pR1_uav_z_yaw_v2_20260828` raw bag、run result 和 navmesh JSON/PNG；137.66 s 正常完成，实际 Z 为 0.31–2.568 m，planner yaw 最大对齐误差 0.039°，free/known coverage 为 99.24%/100.00%。
- 本地无 00377 资产，按可用的 `00337-CFVBbU9Rsyb` 完成同配置探索；新增 BAGS2 `hm3d_stage1_00337-CFVBbU9Rsyb_uav_z_yaw_v1_20260828` raw bag、run result 和 navmesh JSON/PNG。561.14 s 后正常 complete，Z 为 0.134–8.715 m，planner yaw 最大误差 0.336°，单层 free/known coverage 为 96.66%/99.84%。
- 多楼层可视化不再依赖固定楼层数：`visualization_record.launch`、`visualization_replay.launch`、`replay_bag_rviz.sh` 增加 `multi_floor`/`floor_layer_count`；节点按地板点云高度自动创建并锁存从 `/pre_map_vln/floor_1` 开始的楼层话题，`floor_layer_count=0` 表示自动不限层。
- 新增 `scripts/compare_navmesh_map_floors.py`，从 Habitat navmesh 高度分布检测实际楼层，并为每层生成独立对比 PNG/JSON；00337 自动得到 4 层（1.25/3.85/6.45/9.25 m），避免多楼层压到同一张二维图。
- `scripts/build_occusg_grid.py` 与 `scripts/compare_navmesh_map.py` 支持逐层相机高度带、三维 navmesh 采样和单层对齐审计；`visualization_replay.launch`/脚本增加 `start` 起始秒数用于末段回放验证。
- `build_occusg_grid.py` 新增障碍端点高度带与相机帧高度带分离、端点相对相机垂直位移过滤和多帧 occupied 确认；`compare_navmesh_map_floors.py` 默认使用楼层 ±0.45 m 端点带、±1.15 m 相机带和 0.75 m 垂直位移阈值，避免将地板/天花板回波误判为二维占据。
- `run_habitat_falcon.py` 默认在自动识别的最低楼层选择最大障碍净空起点，并把 Habitat 坐标、楼层号、净空和连通岛面积写入 `manifest.json`/`run_result.json`；`record_progressive_stage1.sh` 支持 `STAGE1_START_FLOOR`、`STAGE1_START_SAMPLES` 覆盖默认值。
- 新增 `scripts/comparison_origin.py`：对比图从录制 episode manifest 读取真实 Habitat 起点，避免用随机 navmesh 原点造成官方几何漂移；`compare_navmesh_map.py`/`compare_navmesh_map_floors.py` 支持显式起点参数。
- `compare_navmesh_map.py` 默认由三栏改为官方 navmesh + 记录探索地图两栏，诊断统计继续写入 JSON；已用真实起点重生成 `outputs/BAGS2/hm3d_00337_1000_floor_{1,2,3}_navmesh_vs_map.png`。
- 新增 `patches/falcon/floor_priority.patch`，让 FALCON 在当前楼层 frontier 耗尽后才切换到更高楼层，并保留迟到低楼层 frontier 的回访兜底。
- `containers/falcon_noetic/Dockerfile` 已纳入楼层优先补丁；`hm3d_example.yaml` 默认启用 `floor_priority_enabled`，楼层带宽 0.85 m、最小层间距 1.2 m。
