# Local calibration datasets

FAST-Calib captures and generated results are stored below this directory and
are ignored by Git. Never commit bags, images, point clouds, or calibration
outputs. Keep the final reviewed transform in a small source-controlled config
only after visual and physical validation.
